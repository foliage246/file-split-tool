{
  "name": "file-split-tool",
  "version": "1.0.0",
  "description": "Desktop-optimized file splitting tool",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}